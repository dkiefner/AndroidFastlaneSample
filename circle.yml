dependencies:
  # cache_directories:
  #  - "/opt/circleci/.rvm/gems"
  pre:
    # install and accept sdk components, use one line each to accept the license on each
    #   3 - Android SDK Build-Tools 25.0.2
    - echo y | android update sdk -u -a -t 3
    # 160 - Android Support Repository
    - echo y | android update sdk -u -a -t 160

    # install fastlane
    # - gem install fastlane
test:
  - fastlane build_and_run_unit_tests_debug
